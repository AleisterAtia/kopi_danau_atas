<?php

namespace Tests\Feature;

use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class ExampleTest extends TestCase
{
    use RefreshDatabase;

    /**
     * The homepage relies on `homepage_sections`, `tour_packages`, etc.
     * which need migrations to be present. With RefreshDatabase the
     * sqlite in-memory schema is built fresh per test run.
     */
    public function test_the_application_returns_a_successful_response(): void
    {
        $response = $this->get('/');

        $response->assertStatus(200);
    }
}
